# Base URL for the ROAD.NET backend API.
# When a Vite dev proxy for /api is configured, this can be left at the
# default ('' -> same-origin '/api'). Set to the full backend origin when
# the proxy is removed, e.g. http://localhost:8080/api
#VITE_API_URL=http://localhost:8080/api
